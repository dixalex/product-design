# visual-design

Pre-behavior aesthetic decisions for a product. Walks Mood → Typography → Color → Spacing → Motion → Polish via guided dialogue; produces a visual spec at `<project-root>/docs/visual-design/<date>-<slug>.md`.

See `SKILL.md` for the full skill content. See `references/handoff-spec-template.md` for the output artifact shape.
